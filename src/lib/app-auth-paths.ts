/** Public seller app entry — login/signup only, not the marketing homepage. */
export const APP_AUTH_BASE = "/app";

export const APP_LOGIN_PATH = `${APP_AUTH_BASE}`;
export const APP_SIGNUP_PATH = `${APP_AUTH_BASE}/signup`;
