/** Public seller app entry — Google sign-in only. */
export const APP_AUTH_BASE = "/app";

export const APP_LOGIN_PATH = `${APP_AUTH_BASE}`;
/** @deprecated Use APP_LOGIN_PATH — signup redirects to login. */
export const APP_SIGNUP_PATH = APP_LOGIN_PATH;
